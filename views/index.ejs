<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuildApp - Welcome</title>
    <%- include('partials/head') %>
    <style>
        .welcome-container {
            max-width: 800px;
            margin: 4em auto;
            padding: 2em;
            text-align: center;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .welcome-title {
            font-size: 3em;
            color: #006064;
            margin-bottom: 0.5em;
            font-weight: 900;
        }
        
        .welcome-subtitle {
            font-size: 1.3em;
            color: #666;
            margin-bottom: 2em;
            line-height: 1.5;
        }
        
        .welcome-buttons {
            display: flex;
            gap: 1em;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .welcome-btn {
            padding: 1em 2em;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .welcome-btn.primary {
            background: #006064;
            color: white;
        }
        
        .welcome-btn.secondary {
            background: #f8f9fa;
            color: #006064;
            border: 2px solid #006064;
        }
        
        .welcome-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        
        .features-preview {
            margin-top: 3em;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2em;
        }
        
        .feature-preview {
            text-align: center;
            padding: 1.5em;
            background: #f8f9fa;
            border-radius: 10px;
        }
        
        .feature-preview i {
            font-size: 2em;
            color: #006064;
            margin-bottom: 1em;
        }
        
        .feature-preview h3 {
            color: #333;
            margin-bottom: 0.5em;
        }
        
        .feature-preview p {
            color: #666;
            font-size: 0.9em;
        }
        
        /* Responsive styles */
        @media (max-width: 768px) {
            .welcome-container {
                margin: 2em 1em;
                padding: 1.5em;
            }
            
            .welcome-title {
                font-size: 2.2em;
            }
            
            .welcome-subtitle {
                font-size: 1.1em;
                margin-bottom: 1.5em;
            }
            
            .welcome-buttons {
                flex-direction: column;
                gap: 0.8em;
            }
            
            .welcome-btn {
                width: 100%;
                padding: 0.9em 1.5em;
            }
            
            .features-preview {
                grid-template-columns: 1fr;
                gap: 1.5em;
                margin-top: 2em;
            }
        }
        
        @media (max-width: 480px) {
            .welcome-container {
                margin: 1em 0.8em;
                padding: 1.2em;
            }
            
            .welcome-title {
                font-size: 1.8em;
                margin-bottom: 0.4em;
            }
            
            .welcome-subtitle {
                font-size: 1em;
                margin-bottom: 1.2em;
            }
            
            .welcome-btn {
                padding: 0.8em 1.2em;
                font-size: 0.95em;
            }
            
            .feature-preview {
                padding: 1.2em;
            }
            
            .feature-preview i {
                font-size: 1.8em;
            }
            
            .feature-preview h3 {
                font-size: 1.1em;
            }
            
            .feature-preview p {
                font-size: 0.85em;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="welcome-container">
        <h1 class="welcome-title">Welcome to BuildApp</h1>
        <p class="welcome-subtitle">
            The platform that connects clients with verified professionals. 
            Find the perfect match for your projects or showcase your skills to potential clients.
        </p>
        
        <div class="welcome-buttons">
            <a href="/client/pro-list" class="welcome-btn primary">Find Professionals</a>
            <a href="/client/projects/public" class="welcome-btn secondary">Browse Projects</a>
        </div>
        
        <div class="features-preview">
            <div class="feature-preview">
                <i class="fa-solid fa-users"></i>
                <h3>Verified Professionals</h3>
                <p>Connect with skilled and verified professionals in your area</p>
            </div>
            
            <div class="feature-preview">
                <i class="fa-solid fa-star"></i>
                <h3>Rating System</h3>
                <p>Make informed decisions with our comprehensive rating system</p>
            </div>
            
            <div class="feature-preview">
                <i class="fa-solid fa-folder-open"></i>
                <h3>Project Management</h3>
                <p>Create and manage projects with ease</p>
            </div>
        </div>
    </div>
</body>
</html>